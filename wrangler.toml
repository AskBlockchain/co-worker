# Cloudflare Pages — static export in ./public (run: npm run build)
# Dashboard (Git): Build command = npm run build, Build output = public
# CLI: npm run deploy  (requires: npx wrangler login)

name = "co-worker"
compatibility_date = "2025-03-01"
pages_build_output_dir = "public"
